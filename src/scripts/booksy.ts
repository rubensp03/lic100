export const initBooksy = (id: string) => {
  console.log('Booksy widget prepared for ID:', id);
  // Here we would inject the real Booksy script when the user is ready
  // Example: <script src="https://booksy.com/widget.js" data-id="id"></script>
};
